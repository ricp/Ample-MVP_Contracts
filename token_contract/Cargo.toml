[package]
name = "token_contract"
version = "1.0.0"
authors = ["vote for pedro"]
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
near-contract-standards = "4.0.0"
near-sdk = "4.0.0"